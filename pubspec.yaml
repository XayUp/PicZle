name: piczle
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  image: ^4.8.0
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  pdf: ^3.12.0
  file_picker: ^8.1.3
  archive: ^4.0.9 # Atualizado para ser compatível com image ^4.8.0
  share_plus: ^12.0.2
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
